---

spring:
  datasource:
    url: jdbc:hsqldb:mem:DSCIdentityService
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.HSQLDialect

kafka:
  bootstrapAddress: localhost:9092